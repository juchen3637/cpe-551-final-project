import os
import tkinter as tk
import tkinter.filedialog as fd

def main():
  # file = ""
  # while not os.path.exists(file):
  #   file = fd.askopenfilename(title="",initialdir=os.getcwd())
  # print("The file name is: ", file)
  # with open(file, 'r') as lines:
  #   for line in lines:
  #     if line!=lines[0]:
  #       line = line.strip()
  #       info = list(line.split("\t"))
  #     #flowerInfo[spots[0]] = spots
  my_list = [1, 5, 2, 3, 7]
  print(my_list[1:])
  
main()